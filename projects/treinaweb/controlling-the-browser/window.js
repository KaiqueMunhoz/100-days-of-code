window.close(); //Close the window

let myWin = window.open('www.google.com'); //Open an URL
myWin.close(); //Close the URL open before

window.open('www.google.com', '_blank', 'resizable=no, top=200, left=200, width=300, height=200 ');

//All of these methods just run when they are executed inside a function, or with an event.